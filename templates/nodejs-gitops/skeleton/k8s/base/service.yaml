apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  labels:
    app: ${{ values.appName }}
spec:
  selector:
    app: ${{ values.appName }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080