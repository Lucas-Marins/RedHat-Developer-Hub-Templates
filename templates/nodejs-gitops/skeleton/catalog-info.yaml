apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.appName }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: ${{ values.githubOrg }}/${{ values.appName }}
    backstage.io/kubernetes-namespace: ${{ values.namespace }}
    backstage.io/kubernetes-label-selector: 'app.kubernetes.io/part-of=${{ values.appName }}'
    backstage.io/techdocs-ref: dir:.
    argocd/app-name: ${{ values.appName }}-${{ values.environment }}
    argocd/instance-name: main
  tags:
    - nodejs
    - express
    - ${{ values.environment }}
    - gitops
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}
  system: ${{ values.appName }}-system
---
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: ${{ values.appName }}-system
spec:
  owner: ${{ values.owner }}